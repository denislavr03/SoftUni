function nxnMatrix(num) {
    let arr = [];
    for (let index = 0; index < num; index++) {
        arr.push(num);

    }
    for (let index = 0; index < num; index++) {
        console.log(arr.join(' '));
    }
}

nxnMatrix(3);